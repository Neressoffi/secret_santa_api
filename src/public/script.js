// Définission  des fonctions pour interagir avec le backend via les contrôleurs

async function createGroup() {
    // Implémentation de la logique pour envoyer une requête POST au contrôleur groupController.createGroup
  }
  
  async function inviteMembers() {
    // Implémentation de la logique pour envoyer une requête POST au contrôleur groupController.inviteMembers
  }
  
  async function respondToInvitation() {
    // Implémentation de la logique pour envoyer une requête POST au contrôleur groupController.respondToInvitation
  }
  
  async function deleteGroup() {
    // Implémentation de la logique pour envoyer une requête DELETE au contrôleur groupController.deleteGroup
  }
  
  // Récupération  et affichage des groupes d'administration lorsque la page se charge
  document.addEventListener('DOMContentLoaded', async () => {
    // Implémentation de la logique pour récupérer et afficher les groupes d'administration via le contrôleur groupController.adminGroups
  });
  